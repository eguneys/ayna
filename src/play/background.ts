import { Context, Cus } from '../context';
import * as bs from './bounds';

import Rect from './rect';

export default class Background extends Cus {

  constructor(context: Context) {
    super(context);
  }

  update(dt: number) {
  }

  render() {
  }
  
}
